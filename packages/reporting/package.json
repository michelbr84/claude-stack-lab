{
  "name": "@lab/reporting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. --dir packages/reporting"
  },
  "dependencies": {
    "@lab/shared": "workspace:*",
    "@lab/scoring-engine": "workspace:*"
  }
}
