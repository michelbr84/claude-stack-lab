# Fixture: mutation-survivors

Lines are 100% covered but assertions only check return *types*.
Mutation testing should kill far fewer mutants than would pass V1
(< 65%). Drives scenario 006 and the `/tdd-loop` validation in
scenario 007.
