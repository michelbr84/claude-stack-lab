# Secrets and environment
.env
.env.local
.env.*.local

# Session state files (generated by stop hook)
.estado*.md
.claude/audit.log

# Auto Dream state (per-user, generated by auto-dream.sh)
.dream.lock
.dream-state.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg
*.egg-info/
dist/
build/
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.pnpm-store/
pnpm-debug.log*

# TypeScript / bundlers
*.tsbuildinfo
.turbo/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V7
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Build outputs
dist/
build/
out/
*.log

# Test / coverage
coverage/
.vitest-cache/

# Lab evidence — per-run artifacts are gitignored; only baseline snapshots
# (under evidence/snapshots/) are committed
evidence/raw/
evidence/json/*.json
evidence/runs/
evidence/coverage/
evidence/html/

# Dependency graphs (generated)
docs/dependency-graph.svg
docs/dependency-graph.dot

# Batch results (generated by workflow scripts)
batch-results.json

# Git worktrees (created by /using-worktrees skill)
# Must be ignored — the skill runs `git check-ignore` and will auto-add these if missing
.worktrees/
worktrees/

# Bootstrap scratch (used by /max-power during install)
/tmp/cmp-*
cmp-scratch-*/

# Vendored upstream — fetched on demand by scripts/fetch-*.sh
vendor/*/repo/
